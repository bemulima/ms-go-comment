schema_version: 1
name: refactor
steps:
  - state API, schema, tree, policy, sequence, event, attachment, and frontend behavior that must remain unchanged
  - establish a passing focused baseline
  - refactor one domain, use-case, or adapter boundary at a time
  - keep data or contract changes separate unless explicitly required
  - run repository verification
  - review the final diff for accidental ownership or delivery changes
